import React from 'react';
import styled from 'styled-components';

const WritingHeader = styled.h1`
	font-size: 1.6rem;
`;
const Writing = () => {
	return <WritingHeader>부산여행</WritingHeader>;
};

export default Writing;
